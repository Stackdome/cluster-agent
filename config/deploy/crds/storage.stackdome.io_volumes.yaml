---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: volumes.storage.stackdome.io
spec:
  group: storage.stackdome.io
  names:
    kind: Volume
    listKind: VolumeList
    plural: volumes
    singular: volume
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Volume is the Schema for the volumes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkspaceVolumeSpec defines the desired state of WorkspaceVolume
            properties:
              accessMode:
                type: string
              annotations:
                additionalProperties:
                  type: string
                type: object
              labels:
                additionalProperties:
                  type: string
                type: object
              needsSyncBeforeUse:
                type: boolean
              size:
                minLength: 1
                type: string
              source:
                properties:
                  buildArtifacts:
                    items:
                      description: BuildArtifactSource defines how to copy artifacts
                        from a application built to a volume
                      properties:
                        buildSource:
                          description: |-
                            BuildSource references the stack resource whose build artifacts should be copied.
                            This must reference a StackResource with ApplicationBuildSpec defined.
                          properties:
                            name:
                              description: Name of the StackResource
                              type: string
                          required:
                          - name
                          type: object
                        destinationPath:
                          description: DestinationPath is the path within the volume
                            where artifacts should be synced
                          type: string
                        sourcePath:
                          description: SourcePath is the path within the built image
                            where artifacts are located
                          type: string
                      required:
                      - buildSource
                      - destinationPath
                      - sourcePath
                      type: object
                    type: array
                  gitRepo:
                    properties:
                      auth:
                        properties:
                          personalAccessTokenRef:
                            description: CredentialSecretKeyPair selects username
                              and password keys from a Secret
                            properties:
                              passwordKey:
                                description: PasswordKey is the key in the secret
                                  for the password
                                type: string
                              secretRef:
                                description: |-
                                  SecretReference represents a Secret Reference. It has enough information to retrieve secret
                                  in any namespace
                                properties:
                                  name:
                                    description: name is unique within a namespace
                                      to reference a secret resource.
                                    type: string
                                  namespace:
                                    description: namespace defines the space within
                                      which the secret name must be unique.
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              usernameKey:
                                description: UsernameKey is the key in the secret
                                  for the username
                                type: string
                            required:
                            - passwordKey
                            - secretRef
                            - usernameKey
                            type: object
                          usernamePasswordAuthRef:
                            description: CredentialSecretKeyPair selects username
                              and password keys from a Secret
                            properties:
                              passwordKey:
                                description: PasswordKey is the key in the secret
                                  for the password
                                type: string
                              secretRef:
                                description: |-
                                  SecretReference represents a Secret Reference. It has enough information to retrieve secret
                                  in any namespace
                                properties:
                                  name:
                                    description: name is unique within a namespace
                                      to reference a secret resource.
                                    type: string
                                  namespace:
                                    description: namespace defines the space within
                                      which the secret name must be unique.
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              usernameKey:
                                description: UsernameKey is the key in the secret
                                  for the username
                                type: string
                            required:
                            - passwordKey
                            - secretRef
                            - usernameKey
                            type: object
                        type: object
                      destinationWithinVolume:
                        default: repo
                        description: Destination within the volume where the git repo
                          should be synced
                        type: string
                      repoUrl:
                        type: string
                      revision:
                        description: |-
                          Can be one of the following:
                          - git commit hash
                          - git branch name
                          - git tag name
                        properties:
                          branch:
                            properties:
                              HeadSha:
                                default: HEAD
                                description: |-
                                  Current commit hash of the branch head.
                                  default is head
                                type: string
                              name:
                                description: Name of the branch
                                type: string
                            required:
                            - HeadSha
                            - name
                            type: object
                          commit:
                            type: string
                          tag:
                            type: string
                        type: object
                    required:
                    - destinationWithinVolume
                    - repoUrl
                    type: object
                  remoteDir:
                    properties:
                      currentDirectoryHash:
                        description: We use this to track the current state of the
                          directory
                        minLength: 1
                        type: string
                      path:
                        description: Path within the client where the directory to
                          be synced is located.
                        type: string
                    required:
                    - currentDirectoryHash
                    - path
                    type: object
                type: object
              storageClass:
                type: string
            required:
            - accessMode
            - needsSyncBeforeUse
            - size
            type: object
          status:
            description: VolumeStatus defines the observed state of WorkspaceVolume
            properties:
              buildArtifactSyncs:
                additionalProperties:
                  properties:
                    buildID:
                      type: string
                    status:
                      type: string
                  required:
                  - buildID
                  - status
                  type: object
                type: object
              conditions:
                description: Conditions is a list of status conditions ths object
                  is in.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              gitRepoSyncedPathWithinVolume:
                description: The path within the volume where the git repo was synced
                  to.
                type: string
              lastRemoteSyncHash:
                description: Tracks the last time the volume was synced from a remote
                  g.
                type: string
              lastSyncedGitReference:
                description: |-
                  Can be
                  - a commit hash
                  - a branch name
                  - a tag name
                type: string
              observedGeneration:
                description: The most recent generation observed by the controller.
                format: int64
                type: integer
              phase:
                default: Pending
                type: string
              pvcName:
                type: string
              statusHash:
                description: |-
                  Used to track the status of the volume. This is a hash of the entire status block.
                  This is used by the stackdome api server to determine if the status has changed since
                  it last observed it.
                type: string
            required:
            - pvcName
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
