---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: storages.storage.stackdome.io
spec:
  group: storage.stackdome.io
  names:
    kind: Storage
    listKind: StorageList
    plural: storages
    singular: storage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: This Object is used to describe the storage requirements for
          a stackdome stack.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              provisionedFor:
                type: string
              userPublicSSHKey:
                format: base64
                type: string
              volumeSpecs:
                additionalProperties:
                  description: WorkspaceVolumeSpec defines the desired state of WorkspaceVolume
                  properties:
                    accessMode:
                      type: string
                    annotations:
                      additionalProperties:
                        type: string
                      type: object
                    labels:
                      additionalProperties:
                        type: string
                      type: object
                    needsSyncBeforeUse:
                      type: boolean
                    size:
                      minLength: 1
                      type: string
                    source:
                      properties:
                        buildArtifacts:
                          items:
                            description: BuildArtifactSource defines how to copy artifacts
                              from a application built to a volume
                            properties:
                              buildSource:
                                description: |-
                                  BuildSource references the stack resource whose build artifacts should be copied.
                                  This must reference a StackResource with ApplicationBuildSpec defined.
                                properties:
                                  name:
                                    description: Name of the StackResource
                                    type: string
                                required:
                                - name
                                type: object
                              destinationPath:
                                description: DestinationPath is the path within the
                                  volume where artifacts should be synced
                                type: string
                              sourcePath:
                                description: SourcePath is the path within the built
                                  image where artifacts are located
                                type: string
                            required:
                            - buildSource
                            - destinationPath
                            - sourcePath
                            type: object
                          type: array
                        localDir:
                          properties:
                            continousSync:
                              type: boolean
                            path:
                              type: string
                          required:
                          - continousSync
                          - path
                          type: object
                      type: object
                    storageClass:
                      type: string
                  required:
                  - accessMode
                  - needsSyncBeforeUse
                  - size
                  type: object
                type: object
            required:
            - userPublicSSHKey
            - volumeSpecs
            type: object
          status:
            properties:
              conditions:
                description: Conditions is a list of status conditions ths object
                  is in.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedStackdomeServerObjectGeneration:
                format: int64
                type: integer
              phase:
                default: Pending
                description: Human readable status - please use .Conditions from code
                type: string
              serviceName:
                description: Name of the svc which exposes this storage pod(internally)
                type: string
              statusHash:
                type: string
              volumeStatus:
                items:
                  properties:
                    Available:
                      type: boolean
                    lastSyncedAt:
                      description: Applicable to only syncable volume types.
                      format: date-time
                      type: string
                    subpath:
                      description: Path within the storage pod where the volume is
                        mounted.
                      type: string
                    volumeName:
                      type: string
                  required:
                  - Available
                  - subpath
                  - volumeName
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
