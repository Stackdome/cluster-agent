---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: workspaces.core.stackdome.io
spec:
  group: core.stackdome.io
  names:
    kind: Workspace
    listKind: WorkspaceList
    plural: workspaces
    singular: workspace
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workspace is the Schema for the workspaces API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkspaceSpec defines the desired state of Workspace
            properties:
              domain:
                description: |-
                  SLD+TLD. Example: example.io
                  Should not be empty and is a valid domain.
                type: string
              organisation:
                type: string
              resources:
                items:
                  properties:
                    name:
                      type: string
                    spec:
                      description: WorkspaceResourceSpec defines the desired state
                        of WorkspaceResource
                      properties:
                        args:
                          items:
                            type: string
                          type: array
                        buildSpec:
                          description: Only one of the following fields should be
                            specified
                          properties:
                            buildContext:
                              description: Build Context within the source volume.
                              type: string
                            buildSourceHash:
                              type: string
                            dockerFilePath:
                              description: Path to the docker file within the source
                                volume.
                              type: string
                            registry:
                              description: Registry specification for pushing the
                                built image
                              properties:
                                auth:
                                  properties:
                                    dockerConfigAuth:
                                      properties:
                                        credentialsRef:
                                          description: CredentialSecretKeyPair selects
                                            username and password keys from a Secret
                                          properties:
                                            passwordKey:
                                              description: PasswordKey is the key
                                                in the secret for the password
                                              type: string
                                            secretName:
                                              description: Secret is the name of the
                                                secret
                                              type: string
                                            secretNamespace:
                                              type: string
                                            usernameKey:
                                              description: UsernameKey is the key
                                                in the secret for the username
                                              type: string
                                          required:
                                          - passwordKey
                                          - secretName
                                          - usernameKey
                                          type: object
                                      type: object
                                    type:
                                      type: string
                                  required:
                                  - type
                                  type: object
                                insecure:
                                  default: false
                                  description: Is the registry insecure
                                  type: boolean
                                repositoryUrl:
                                  description: Repository URL for constructing the
                                    image tag (e.g., docker.io/myorg)
                                  type: string
                              required:
                              - repositoryUrl
                              type: object
                            sourceVolumeName:
                              description: Source volume where the build context is
                                present.
                              type: string
                          required:
                          - buildContext
                          - buildSourceHash
                          - dockerFilePath
                          - registry
                          - sourceVolumeName
                          type: object
                        command:
                          items:
                            type: string
                          type: array
                        dependsOn:
                          description: Other resources this workspace resource depends
                            on.
                          items:
                            type: string
                          type: array
                        environmentVariables:
                          items:
                            properties:
                              name:
                                type: string
                              value:
                                type: string
                            required:
                            - name
                            - value
                            type: object
                          type: array
                        imageSpec:
                          properties:
                            image:
                              description: Image reference (e.g., docker.io/myorg/myimage:tag)
                              type: string
                            pullAuth:
                              description: Registry authentication for pulling the
                                image
                              properties:
                                dockerConfigAuth:
                                  properties:
                                    credentialsRef:
                                      description: CredentialSecretKeyPair selects
                                        username and password keys from a Secret
                                      properties:
                                        passwordKey:
                                          description: PasswordKey is the key in the
                                            secret for the password
                                          type: string
                                        secretName:
                                          description: Secret is the name of the secret
                                          type: string
                                        secretNamespace:
                                          type: string
                                        usernameKey:
                                          description: UsernameKey is the key in the
                                            secret for the username
                                          type: string
                                      required:
                                      - passwordKey
                                      - secretName
                                      - usernameKey
                                      type: object
                                  type: object
                                type:
                                  type: string
                              required:
                              - type
                              type: object
                          required:
                          - image
                          type: object
                        init:
                          properties:
                            args:
                              items:
                                type: string
                              type: array
                            command:
                              items:
                                type: string
                              type: array
                          required:
                          - command
                          type: object
                        ports:
                          items:
                            properties:
                              exposeToPublic:
                                default: false
                                type: boolean
                              isHttp:
                                default: true
                                type: boolean
                              number:
                                format: int32
                                type: integer
                              subdomain:
                                type: string
                            required:
                            - number
                            - subdomain
                            type: object
                          type: array
                        restartRequest:
                          format: date-time
                          type: string
                        stateFul:
                          type: boolean
                        volumeMounts:
                          items:
                            properties:
                              destination:
                                type: string
                              sourceSubPath:
                                type: string
                              sourceWorkspaceVolume:
                                type: string
                            required:
                            - destination
                            - sourceSubPath
                            - sourceWorkspaceVolume
                            type: object
                          type: array
                      type: object
                  required:
                  - name
                  - spec
                  type: object
                type: array
              userName:
                type: string
            required:
            - domain
            - organisation
            - resources
            - userName
            type: object
          status:
            description: WorkspaceStatus defines the observed state of Workspace
            properties:
              conditions:
                description: Conditions is a list of status conditions ths object
                  is in.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              observedStackdomeServerObjectGeneration:
                description: The most recent generation observed by the controller.
                format: int64
                type: integer
              phase:
                default: Pending
                type: string
              statusHash:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
