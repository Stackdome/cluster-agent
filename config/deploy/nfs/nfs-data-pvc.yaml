apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-data
  namespace: stackdome-control-plane
spec:
  accessModes:
    - ReadWriteOnce
  # storageClassName: "standard-rwo"
  resources:
    requests:
      storage: 100Gi

