---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: clusterregistries.registry.stackdome.io
spec:
  group: registry.stackdome.io
  names:
    kind: ClusterRegistry
    listKind: ClusterRegistryList
    plural: clusterregistries
    singular: clusterregistry
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner.type
      name: Owner Type
      type: string
    - jsonPath: .spec.owner.id
      name: Owner ID
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.internalUrl
      name: Internal URL
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRegistry is the Schema for the cluster registries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRegistrySpec defines the desired state of ClusterRegistry
            properties:
              auth:
                description: Authentication configuration
                properties:
                  htPasswordCredentials:
                    description: Basic authentication credentials for the registry
                    properties:
                      credentialsRef:
                        description: CredentialSecretKeyPair selects username and
                          password keys from a Secret
                        properties:
                          passwordKey:
                            description: PasswordKey is the key in the secret for
                              the password
                            type: string
                          secretRef:
                            description: |-
                              SecretReference represents a Secret Reference. It has enough information to retrieve secret
                              in any namespace
                            properties:
                              name:
                                description: name is unique within a namespace to
                                  reference a secret resource.
                                type: string
                              namespace:
                                description: namespace defines the space within which
                                  the secret name must be unique.
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          usernameKey:
                            description: UsernameKey is the key in the secret for
                              the username
                            type: string
                        required:
                        - passwordKey
                        - secretRef
                        - usernameKey
                        type: object
                    required:
                    - credentialsRef
                    type: object
                required:
                - htPasswordCredentials
                type: object
              owner:
                description: Owner information
                properties:
                  id:
                    description: ID of the owner
                    minLength: 1
                    type: string
                  type:
                    description: Type of the owner (e.g., "Organization", "Team")
                    enum:
                    - Organization
                    - Team
                    type: string
                required:
                - id
                - type
                type: object
              port:
                description: Port on which the registry will be running
                format: int32
                maximum: 65535
                minimum: 1000
                type: integer
              retentionPolicy:
                description: Retention policies for the registry
                properties:
                  deleteUntagged:
                    default: true
                    description: Whether to delete untagged manifests
                    type: boolean
                  maxRepositoryCount:
                    description: MaxRepositoryCount is the maximum number of repositories
                      to keep
                    format: int32
                    type: integer
                  tagsPerRepo:
                    description: Number of tags to keep per repository
                    format: int32
                    type: integer
                type: object
              storage:
                description: Storage configuration for the registry
                properties:
                  size:
                    description: Size of the registry storage
                    pattern: ^[0-9]+(Gi|Ti)$
                    type: string
                  storageClass:
                    description: StorageClass to use for registry volume
                    type: string
                required:
                - size
                type: object
            required:
            - owner
            - port
            - storage
            type: object
          status:
            properties:
              conditions:
                description: Conditions is a list of status conditions for the registry
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              internalUrl:
                description: Internal registry URL for workspaces to use
                type: string
              observedGeneration:
                description: The most recent generation observed by the controller
                format: int64
                type: integer
              phase:
                default: Pending
                description: Phase represents the current state of the registry
                type: string
              serviceIp:
                description: ServiceIP is the cluster IP of the registry svc.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
